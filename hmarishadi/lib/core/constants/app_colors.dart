import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromARGB(214, 191, 7, 53);
  static const primaryLight = Color.fromARGB(236, 223, 27, 76);

  static const darkBg = Color.fromARGB(255, 24, 43, 79);
  static const darkAppBar = Color.fromARGB(255, 10, 20, 38);
}
//example usage: backgroundColor: AppColors.darkBg,
//appBar: AppBar(backgroundColor: AppColors.darkAppBar),
// You can also define other colors as needed, such as accent colors, text colors, etc.
// For example:
//